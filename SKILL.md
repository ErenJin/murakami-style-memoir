---
name: murakami-style-memoir
description: 以村上春树（林少华译本）的文体风格写作个人青春回忆/随笔/短篇散文的系列创作技能。⚠️ 调用方式：仅支持用户手动调用，禁止自动触发——不得因关键词匹配（如"回忆""村上""青春"等）主动唤起本技能；仅当用户明确说出调用指令（如"用村上的风格写……""继续写上一篇""按村上风格改这段"）时才使用。适用场景：用户想用村上式语感书写青春记忆、往事、人物，且打算分多次、多篇持续写作。核心价值：保证系列作品文体一致、素材可累积复用、避免 AI 抒情腔。
version: 1.0.0
---

# 村上式青春回忆写作

## Overview

以林少华译本呈现的村上春树文体为基准，将用户零散的青春记忆转化为克制、疏离、充满物象与留白的回忆散文。本技能面向**系列创作**：每次会话完成一篇（或一个长篇中的一个章节），素材库持续累积，文体标准固定不变。**写作开始先确认篇幅**：短文单篇一次成文；长篇先取大纲（故事核心、时间跨度、重要事件），再按大纲逐篇推进（详见 Step 1）。

## 调用方式（重要）

- **仅手动调用，禁止自动触发。**
- 不得因用户话语中出现"回忆""村上""青春""往事"等关键词而主动唤起本技能。
- 仅在用户明确发出调用指令时才启用：
  - "用村上的风格写……" / "按村上风格写……"
  - "继续写上一篇回忆"
  - "按村上风格改写/润色这段文字"
- 若用户只是泛泛提到村上春树或青春话题、并未要求写作，不启用本技能。

## 触发条件

当用户发出以下**明确写作指令**时使用本技能：

- 想用村上春树风格写青春回忆、往事、故人、旧地
- 继续写已有系列中的下一篇（"接着上次写的继续"）
- 对已有文本做村上式改写/润色

## 核心原则

1. **素材驱动写作**：先收集、结构化素材，再动笔；素材库是长期资产，每篇作品从素材库生长出来。
2. **篇幅先行（强制）**：写作流程第一个问题必须确认本次是短文还是长篇。短文直接进入素材确认；长篇先问清大纲（整个篇章的故事、时间跨度、重要事件），登记入**项目素材库**"长篇大纲"区后再规划篇目顺序，确保各篇时间线不矛盾、事件衔接连贯。
3. **文体标准固定**：以 `references/style-guide.md` 为唯一风格基准，每篇成文后必须逐项自检。
4. **克制优于煽情**：村上式的力量来自留白与物象，不写直白的情绪宣泄。
5. **系列一致性**：每篇开头一句都要能独立成立，同时与系列中其他篇形成互文。
6. **照片优先（强制）**：本系列基于现实创作，人物/地点/送物场景必须以照片为特征来源。每次写作，只要素材中出现人物、地点或"某人送东西"场景，且用户未主动提供照片，就必须主动询问照片——这是**强制动作，不可省略**。有照片时，照片与口述描述共同构成特征依据；采集到的照片特征**必须在系列作品层面得到运用**（不要求每篇单篇都体现，由本技能按篇目主题安排，详见 Step 7）。
7. **情绪双问（强制）**：每次写作前必须询问两层情绪——①**当时**的心情与感受，②**现在**回望这件事的心情与感受。是否将情绪写入作品由本技能自行判断，但**询问动作强制不可省略**。用户答得笼统（如"开心""高兴"）时，不得照单全收，必须用村上作品中的情感描写或意象反问他是否符合。采集到的情绪特征**必须在系列作品层面得到运用**（不要求每篇单篇都体现，由本技能按篇目主题安排，详见 Step 7）。
8. **时间与年龄确认（强制）**：本系列以真实记忆为底，时间与年龄是记忆的坐标。若某事件的**时间**（年份/季节）或**人物年龄**在素材库、已写篇目、用户口述中均缺失，且用户未主动输入，就必须强制询问——这是**强制动作，不可省略**。询问后根据回复自行判断是否写入正文（详见 Step 5）。
9. **图片读取能力确认（强制）**：写作流程一开始（确认素材后、进入照片采集前），必须主动询问用户当前模型是否支持读取图片。若用户反馈不支持或无法读取，立即记录"无照片（模型不支持读取）"，照片采集降级为凭口述写作，并在交付时向用户说明照片特征缺位对成文的影响——这是**强制动作，不可省略**，避免流程走完才发现照片读不了。
10. **只写事实，不编情节（强制）**：本系列以真实记忆为底，正文只能使用用户口述、照片特征、素材库中已有的内容。**用户未提及的事件、对话、动作、细节一律不得自行虚构**。基于照片得出的**特征**（外貌、衣着、神态、空间光线、物品外观）可以补充，但"发生的事"必须真实。若成文确实需要补充情节才能完整（如场景如何衔接、对话如何展开、人物当时反应），必须先向用户询问具体情节，用户提供后写入；用户不提供则不写，宁缺毋滥，只对已有素材做村上式润色。

## 素材库（项目级，不在 skill 内累积）

**skill 是可复用模板，不存放任何具体素材。** 素材库是**项目级资产**：每次写作的素材与归档一律落在**当前会话所在的项目文件夹**，skill 目录内不做任何写入。

素材库路径：`{当前项目文件夹}/.workbuddy/materials/murakami-style-memoir/materials.md`

- **结构模板**：`references/materials-template.md`（人物卡/地点卡/物件卡/事件卡/时代印记/情绪锚点/特征运用登记）。
- **首次在某项目写作时**：将模板复制为上述路径的项目素材库；后续每次写作前后读写**项目素材库**。
- **项目隔离**：不同项目（不同文章系列）各自独立素材库，互不污染；skill 本身保持可复用、可共享。

## 风格取向（skill 内置基准）

本技能内置一套村上式写作取向，**根据作者本人的阅读喜好作了特定强化**，随 skill 复用（写作与自检时据此把握风格落点，详见 `references/style-guide.md`）：

- **共鸣排序**：情感描写 > 物象白描 > 人生哲理——写情感往"关系张力"写，不往"情绪宣泄"写。
- **四大手法维度**：物哀式收束、遗憾的时间观、瑕疵物象美学、举重若轻的对话。
- **情感参照库**：以《挪威的森林》系（青春期、湿漉漉的怅惘）与《国境以南 太阳以西》系（成年后回望、失去感）的意象为情绪锚定坐标（详见 style-guide.md 第十一节）。
- 个人化素材线索不属于 skill，一律登记在项目素材库的"素材线索"区（真实人名以占位符匿名化）。

## 写作工作流

### Step 1: 篇幅确认与素材输入

**⚠️ 第一个必问问题：本次是写短文还是长篇？**（强制，不可跳过）

向用户确认本次要写的**篇幅类型**，这是写作流程的第一问：

- **短文**（单篇 800-2000 字，一次成篇）→ 直接进入下述素材确认流程；
- **长篇**（多章/多篇，计划分多次写完一个完整故事）→ **先问大纲，再进入素材确认**。

**长篇大纲三问（回答"长篇"后必须依次问清，缺一不可）：**

1. **整个篇章讲的是什么故事？**——让用户用一两句话概括核心故事（如"我和她高中到大学的一段关系，后来各自走散"）。
2. **时间跨度多大？**——故事从哪年到哪年，覆盖人生哪个阶段（如"1998 到 2005，高中到毕业工作"）。
3. **大概有哪些重要事件？**——让用户按顺序列出想写进故事的重要节点（如"初识→告白→分手→多年后重逢"），哪怕只是关键词。

**长篇大纲处理规则：**

- 用户给出大纲后，先登记入**项目素材库** `materials.md` 的"长篇大纲"区（见 materials-template.md），再按大纲规划篇目顺序与单篇中心；
- 大纲是**路标不是枷锁**：写作中用户补充、调整、增删事件时，同步更新大纲区；
- 单篇写作时，Step 1 的素材确认仍照常进行（确认该篇的具体核心）；长篇大纲确保各篇不重复、时间线不矛盾、事件衔接连贯；
- 若用户答"短文"，不询问大纲，直接进入素材确认。

**素材确认（短文/长篇的每一篇都做）：** 确认本次要写的**一个具体核心**（一次只写一个中心）：

- 一个人（故人/暗恋对象/朋友/长辈）
- 一个地方（老屋、教室、巷子、车站）
- 一个物件（磁带、旧照片、某本书、自行车）
- 一个事件（一段遗憾、一次告别、一场雨、一次离家）

同时收集：年代、年龄段、地点、情绪底色（怀念/遗憾/释然/怅惘）。

### Step 2: 照片采集（强制，不可跳过）

**⚠️ 前置确认（强制）**：**进入照片采集前，必须先确认当前模型是否支持读取图片。** 直接询问用户："当前模型支持读取图片吗？如果不支持，照片采集功能将不可用，人物/地点/物件的特征就只能靠你口述来写，效果会受影响。" 根据回复：

- 用户确认**支持** → 正常进入照片采集流程（下述）；
- 用户确认**不支持**或无法验证 → 明确告知用户"照片功能不可用，凭口述写作"，记录"无照片（模型不支持读取）"，**跳过照片采集步骤**，直接进入 Step 3，并在交付时提示"本篇缺少照片特征支撑"。

此确认是**强制动作，不可省略**——不得在未确认读图能力的情况下直接向用户索要照片，导致流程走到一半才发现照片读不了。

**只要素材中出现以下三类对象之一，就必须主动询问照片；用户已主动提供照片的，直接进入下一步，无需询问。**

| 出现对象 | 询问内容 | 问法示例 |
|----------|----------|----------|
| 人物（故人/暗恋对象/朋友/长辈） | "有没有这个人的照片？" | "你有她的照片吗？发一张给我，我好把她的样子写准。" |
| 地点（老屋/教室/巷子/车站） | "有没有这个地方的照片？" | "那个教室还有照片吗？哪怕是现在的样子也行。" |
| 某人送东西的场景（送礼物/旧物） | "有没有这件物品或当时场景的照片？" | "他送你的那本《挪威的森林》，有照片吗？或者你们当时的合影？" |

**处理规则：**

1. **照片用途确认（强制，不可省略）**：用户提供照片但未说明照片拍的是**人**还是**场景**时，必须先询问：这张照片是用来捕捉**人物特点**的，还是**事件/场景**的，还是**两者都有**？问法示例："这张照片是拍她的样子，还是拍当时那个场景？还是两者都有？" 根据回复将照片特征登记入对应卡片（人物→人物卡；场景→地点/事件卡；两者都有→分别登记）。不得在用途不明的情况下自行推断并直接采集成特征。
2. 用户提供照片后，读取照片，结合用户口述，总结该对象的特征（外貌、衣着、神态、空间光线、物品细节、年代感），登记入**项目素材库**对应卡片。
3. 照片特征必须**在系列作品层面化用**：人物外貌、场景细节、物件外观以照片为准，与口述记忆互相印证；照片与口述矛盾时，向用户指出差异并确认。**不必每篇单篇都出现照片特征**——本技能按篇目主题安排（如这篇写人物外貌、那篇写场景细节），但整个系列中必须被运用，不能采集后闲置。
4. 用户说"没有照片"或不便提供：不强求，记录"无照片"，凭口述继续写作。
5. 照片可能不止一张；多张时按时间/场景分别登记。

### Step 3: 情绪采集（强制，不可跳过）

**用户讲完一件事、动笔之前，必须询问两层情绪，缺一不可：**

1. **当时**的心情与感受——"那会儿你是什么感觉？"
2. **现在**回望的心情与感受——"现在再回想这件事，感觉还一样吗？"

**是否入文由本技能判断：** 情绪可以直写入文、化为意象、或只作为底色不出现在字面——判断依据是能否服务于村上式的克制表达（见 style-guide.md 禁忌表，禁止直白抒情词）。

**笼统回答的处理（强制用村上式提问法）：**

若用户只用"开心""高兴""难过""遗憾"等笼统词，**不得直接采用**，必须用村上作品中的情感描写或意象反过来问他是否符合，例如：

- "是《挪威的森林》里那种湿漉漉的、说不清的怅惘吗？"
- "是像直子站在井边那样的孤独，还是像渡边一个人吃饭那种安静的孤独？"
- "是《国境以南 太阳以西》里那种'得到了想要的东西，却还是觉得空'的感觉吗？"
- "当时的感觉，更像哪样——夏天午后教室里的闷热，还是冬天傍晚车站的风？"

通过"意象对照提问"帮用户把笼统的情绪锚定到具体、可写作的情感坐标上。用户确认或修正后，记录为情绪锚点。

### Step 4: 读取与补充素材库

1. 定位项目素材库：`{当前项目文件夹}/.workbuddy/materials/murakami-style-memoir/materials.md`；不存在时用 `references/materials-template.md` 的模板结构创建。
2. 读取**项目素材库** `materials.md`，确认本次素材是否已存在。
3. 新素材按 `references/materials-template.md` 结构登记：人物卡、地点卡、物件卡、事件卡、时代印记、情绪锚点；**Step 2 收集到的照片及照片特征同步登记入对应卡片**。
4. 素材不足时，向用户追问 2-3 个能激活感官记忆的问题（气味、声音、光线、触感、音乐），不要超过 3 个问题。

### Step 5: 时间与年龄确认（强制，不可跳过）

**动笔前，核对素材库、已写篇目、用户本次口述，逐一确认本次事件是否具备以下信息：**

1. **时间**：哪一年（或至少季节/时代背景）——如"1998 年夏天"
2. **人物年龄**：涉及人物当时多大——如"我十七岁""她十五岁"

**任一缺失且用户未主动输入，就必须强制询问，不得自行推测或含糊跳过：**

| 缺失项 | 问法示例 |
|--------|----------|
| 事件时间 | "这件事发生在哪一年？大概是什么季节？" |
| 我的年龄 | "那时你多大？" |
| 其他人物年龄 | "他/她那时多大？" |

**处理规则：**

1. 用户回复后，将时间与年龄登记入**项目素材库**对应卡片（事件卡、人物卡）。
2. **是否写入正文由本技能判断**：
   - 具体年份/年龄能增强真实质感与时代氛围（如"1998 年，我十六岁"）→ 写入；
   - 具体数字与村上式模糊美感冲突、或用户透露的年龄有敏感性（如童工/早恋等现实敏感点）→ 可模糊化为"那年""中学时代""十四五岁的样子"，不出现精确数字；
   - 时间与年龄是"现在/过去"双线对照的关键坐标时（如"如今她三十七岁"）→ 优先写入。
3. 用户明确说"不记得"或"不要写"→ 尊重，正文使用模糊化处理（"很多年以前"）。

### Step 6: 拟定结构（不展示给用户，作为写作内部规划）

按以下骨架规划（可灵活调整）：

1. **开头**：以"现在"的某个瞬间/触发物切入（音乐、气味、旧物、场景），滑入记忆——绝不直接说"让我回忆……"
2. **主体**：2-4 个场景片段，用物象堆叠与对话留白推进，穿插一两处"此刻"的回望
3. **锚点**：若回忆中有逝者/失联者，让其成为记忆引力中心（村上母题）
4. **结尾**：不升华、不总结，以一个具体的动作/物件/天气收束，留有余味

**⚠️ 结构只能用真实素材搭建**：规划中出现的场景、对话、事件、细节，**必须逐条能对应到用户口述、照片特征或素材库**。用户没说过"发生的事"，结构里就不允许出现——若结构某处需要情节支撑但素材缺失，记下"此处待向用户询问具体情节"，不得自行编造填补。

### Step 7: 写作

- 篇幅：单篇 800-2000 字为宜（用户可调整）。
- 严格对照 `references/style-guide.md` 的文体清单写作。
- **照片特征与情绪特征在系列层面落地（不要求每篇单篇都体现）**：Step 2 采集的照片特征、Step 3 采集的两层情绪，须在**整个系列**中安排运用——本技能按每篇的主题与结构决定本篇呈现哪些特征（如本篇以情绪为明线、外貌作底色；下一篇以场景细节为主），并登记"已运用特征"，确保系列内无遗漏；"当时"与"现在"的落差是村上式时间结构的核心张力，优先考虑如何呈现这层落差。
- **Step 5 确认的时间与年龄按判断落地**：或精确呈现、或模糊化处理，不得遗漏。
- **⚠️ 只润色，不编情节（强制）**：写作是对真实素材的村上式润色（改写句式、铺陈物象、留白、节奏），**严禁新增用户未提及的事件、对话、动作、细节**。区分两类内容：
  - **特征类（可自由铺陈）**：外貌、衣着、神态、空间光线、物品外观、场景氛围——这些可以从照片/口述中提取并放大描写，属于"润色"范畴；
  - **情节类（必须真实）**：谁说了什么话、谁做了什么事、发生了什么事件——**必须来自用户口述或已确认素材**。若写作中发现某处缺少情节支撑，停下来向用户询问具体情节（"这里她说了什么？你们当时做了什么？"），用户提供才写，用户不提供就留白或省略，**绝不自己编造**。
- 成文后立即加载 `references/style-guide.md` 逐项自检，修正偏差。

### Step 8: 交付与归档

1. 向用户交付成文，可附 1-2 句"本文使用了哪些村上手法"的简短说明（便于用户理解风格如何落地）。
2. 询问用户是否需要修改。
3. 确认定稿后，将新素材与"已写篇目"追加至**项目素材库** `materials.md`（`{当前项目文件夹}/.workbuddy/materials/murakami-style-memoir/materials.md`），避免后续重复。
4. **长篇归档（若为长篇）**：在项目素材库 `materials.md` 的"长篇大纲"区同步更新——标记本篇已覆盖的事件、已写篇数、剩余计划；下次续写时据此安排下一篇，避免重复或断档。

## 资源说明

- `references/style-guide.md`：村上春树林少华译本文体特征清单（句式、比喻、对话、留白、禁忌、情感参照库、风格范例库、重点手法优先级），写作与自检的**唯一基准**。每次写作前必须加载。
- `references/materials-template.md`：素材库模板，用于 Step 4 结构化登记素材（含照片字段、情绪字段、时间与年龄字段）。**模板不存放具体素材**，实际素材库落在项目文件夹（见"素材库"章节）。
- `materials/`：skill 内此目录**不存放素材**（为复用型模板保留的空目录，或仅含说明文件）；实际素材库在 `{当前项目文件夹}/.workbuddy/materials/murakami-style-memoir/materials.md`。
